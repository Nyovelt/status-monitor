# Environment Configuration for Status Monitor
# Copy this file to .env and update with your actual values

# ============================================
# Domain Configuration
# ============================================

# Your primary domain (without protocol)
DOMAIN=status.example.com

# API subdomain (for separate subdomain setup)
API_DOMAIN=api.status.example.com

# ============================================
# Application URLs
# ============================================

# Option 1: Separate Subdomains (Recommended for production)
NEXT_PUBLIC_API_URL=https://api.status.example.com
NEXT_PUBLIC_WS_URL=wss://api.status.example.com/ws/live

# Option 2: Single Domain with Paths (Uncomment if using this option)
# NEXT_PUBLIC_API_URL=https://status.example.com/api
# NEXT_PUBLIC_WS_URL=wss://status.example.com/ws/live

# Option 3: Local Development (Uncomment for local dev)
# NEXT_PUBLIC_API_URL=http://localhost:8080
# NEXT_PUBLIC_WS_URL=ws://localhost:8080/ws/live

# ============================================
# Database Configuration
# ============================================

DATABASE_URL=sqlite:/app/data/monitor.db

# ============================================
# Logging Configuration
# ============================================

# Rust backend logging
# Options: error, warn, info, debug, trace
RUST_LOG=server=info,tower_http=info

# ============================================
# Cloudflare Configuration (Optional)
# ============================================

# Only needed if using Cloudflare DNS-01 challenge for certificates
# Get token from: Cloudflare Dashboard → My Profile → API Tokens
# CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here

# ============================================
# Image Configuration
# ============================================

# Docker image registry and tags
REGISTRY=ghcr.io
IMAGE_OWNER=nyovelt
IMAGE_TAG=latest

# Full image names (auto-constructed)
SERVER_IMAGE=${REGISTRY}/${IMAGE_OWNER}/status-monitor-server:${IMAGE_TAG}
WEB_IMAGE=${REGISTRY}/${IMAGE_OWNER}/status-monitor-web:${IMAGE_TAG}
